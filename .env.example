# ========================================
# MemOS + LangChain 环境变量配置
# ========================================
# 复制此文件为 .env 并填入你的 API 密钥

# OpenAI API 配置 (必需)
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_API_BASE=https://api.openai.com/v1

# MOS 聊天模型配置 (用于 internet_retriever 等组件)
MOS_CHAT_MODEL=gpt-4o-mini
MOS_CHAT_TEMPERATURE=0.8
MOS_MAX_TOKENS=8000
MOS_TOP_P=0.9
MOS_TOP_K=50

# MemReader 配置
MEMRADER_MODEL=gpt-4o-mini
MEMRADER_MAX_TOKENS=8000
MEMRADER_API_KEY=your-openai-api-key-here
MEMRADER_API_BASE=https://api.openai.com/v1

# Embedder 配置 (使用 OpenAI embedding)
MOS_EMBEDDER_BACKEND=universal_api
MOS_EMBEDDER_PROVIDER=openai
MOS_EMBEDDER_MODEL=text-embedding-3-small
MOS_EMBEDDER_API_KEY=your-openai-api-key-here
MOS_EMBEDDER_API_BASE=https://api.openai.com/v1

# 向量维度配置 (text-embedding-3-small = 1536)
EMBEDDING_DIMENSION=1536

# 聊天模型列表配置 (必需用于 init_server)
# 这是一个 JSON 格式的配置列表
CHAT_MODEL_LIST=[{"backend": "openai", "model_name_or_path": "gpt-4o-mini", "temperature": 0.8, "max_tokens": 8000, "api_key": "your-openai-api-key-here", "api_base": "https://api.openai.com/v1"}]

# Neo4j 数据库配置 (用于树形记忆，可选)
NEO4J_BACKEND=neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-neo4j-password-here

# Tavily 搜索 API 配置 (可选)
# 仅在运行 simple_langchain_agent.py 时需要
# 获取地址: https://tavily.com/
TAVILY_API_KEY=your-tavily-api-key-here

# 其他可选配置
ENABLE_PREFERENCE_MEMORY=false
ENABLE_CHAT_API=false
ENABLE_INTERNET=false
API_SCHEDULER_ON=false
INCLUDE_EMBEDDING=false

MOS_ENABLE_REORGANIZE=true
MOS_TEXT_MEM_TYPE=general_text
ASYNC_MODE=sync
